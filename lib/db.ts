/**
 * Prisma removed. User/settings data is handled by SuperYou BE.
 * Stubs for legacy callers; consider migrating to BE APIs.
 */

export async function isUnique(): Promise<boolean> {
  return true;
}

export async function getSettings(): Promise<null> {
  return null;
}
