# SuperYou BE API (block_types, etc.). Include /api/v1 in the URL.
# When set, block types are loaded from this API; when unset, static definitions are used.
# NEXT_PUBLIC_SUPERYOU_API_BASE=https://your-superyou-be-domain.com/api/v1
